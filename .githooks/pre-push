#!/usr/bin/env bash
set -euo pipefail

if [[ "${SKIP_CI_HOOKS:-}" == "1" ]]; then
  echo "pre-push: SKIP_CI_HOOKS=1 set; skipping checks."
  exit 0
fi

echo "pre-push: running CI checks (fmt + clippy + tests)..."
make ci
